version: '3'
services:
  flaskapp:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./:/home/app/
    depends_on:
      - database
    # restart_policy:
    #     condition: on-failure
    # restart: always
  database:
    image: postgres:10
    env_file: .env
    expose:
      - 5432
